export enum PREVIEW_FLOW {
  HOME = 'home',
  INIT_ATTENDANCE = 'init-attendance',
  TERMS_OF_USE = 'terms-of-use',
  CONSENT = 'consents',
  SIGNATURE = 'signature',
  SERVER_DRIVEN_UI = 'server-driven-ui',
}
